import { Just_Another_Hand, Inter } from 'next/font/google'

export const just_Another_Hand = Just_Another_Hand({
  subsets: ['latin'],
  variable: '--font-just-another-hand',
  display: 'swap',
  weight: '400'
})

export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})